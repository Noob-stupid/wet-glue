# 贡献指南 Contributing

感谢你考虑为 wet-glue 做贡献。

## 跑通本地环境

需要 JDK 17+。clone 后：

```bash
./glue list            # 首次自动编译工具自身
bash tests/run.sh      # 39 项全链路断言，应全绿
```

## 提交前

1. `bash tests/run.sh` 必须通过。
2. 新行为请同步补测试断言（tests/run.sh 是纯 bash，照现有断言格式加即可）。
3. 提交信息用中文或英文均可，写清"做了什么 + 为什么"。

## 设计哲学（改代码前请读）

- **极简优先**：头尾两行注释是底线。任何让接入变复杂的特性，默认拒绝。
- **诚实优先**：启发式分析的局限（如 leaks 无类型信息）写进文档，不假装精确。
- **运行时零开销是信仰**：不做运行时代理/反射适配。适配代码写进候选片段，可编译、可 review。
- **防误操作**：固化要验证 + 人工确认，CI 默认拒绝。

## 报告问题

Issue 里请附：操作系统、JDK 版本、复现步骤、`glue check` 输出。
